const pdfjsLib = require('pdfjs-dist');

async function testPdf() {
    // We don't have a specific PDF on hand, but we can write a generic parsing block to see if it compiles or use it for code logic
    console.log("PDFJS is loaded");
}

testPdf();
